# ERP Frontend — Environment Variables
# Copy file này thành .env rồi điền giá trị thực tế

# ── API Backend ──────────────────────────────────────────────
# URL của backend NestJS (KHÔNG có /api ở cuối)
VITE_API_URL=https://erp-fmcg-backend.onrender.com
